import 'package:flutter/material.dart';

class OdersPage extends StatelessWidget {
  const OdersPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('oders'));
  }
}
